
// TC-Time: O(1)
// SC-Space: O(1)

class Solution {
public:
    bool isSameAfterReversals(int num) {
        // If num is 0, it remains the same after reversals
        if (num == 0) return true; 
        
        // Check if num has trailing zeros
        return num % 10 != 0;
    }
};
